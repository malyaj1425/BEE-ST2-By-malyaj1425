<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <style>
      body{
        background-image: url("");
        background-repeat: no-repeat;
        background-size: cover;
        background-color: black;
      }
      @media screen and (max-width:1280px){
        body{
          background-repeat: repeat-y;
        }
      }
      table{
        color: aqua;
        width: 100%;
      }
      table th,td{
        padding: 10px;
      }
      .tble{
        margin-top: 50px;
        margin-left: 50px;
        margin-right: 50px;
      }
    </style>
    <script>
      function close_window() {
        if (confirm("Are you sure you wish to logout?")) {
          close();
        }
      }
    </script>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-danger">
        <a class="navbar-brand" href="#">AUTH3NTICATION</a>
        
      
        <div  id="navbarTogglerDemo02">
          <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item active">
              <a class="nav-link" href="/signinback?id=<%=result[0].id%>" target="_parent">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/data?id=<%= result[0].id %>">View Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/update?id=<%= result[0].id %>">Settings</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" onclick="close_window()">Logout</a>
            </li>
          </ul>
          
        </div>
      </nav>
      <div class="tble">
      <table border="2">
        <tr>
          <th>ID</th>
          <th>USERNAME</th>
          <th>NAME</th>
          <th>GENDER</th>
          <th>PHONE NUMBER</th>
          
          
      </tr>
        <tr>
          <td><%= result[0].id %></td>
          <td><%= result[0].username %></td>
          <td><%= result[0].fname %></td>
          <td><%= result[0].gender %></td>
          <td><%= result[0].pno %></td>      
      </tr>
      </table>
    </div>
</body>
</html>